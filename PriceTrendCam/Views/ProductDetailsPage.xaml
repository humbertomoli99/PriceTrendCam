<Page
    x:Class="PriceTrendCam.Views.ProductDetailsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:behaviors="using:PriceTrendCam.Behaviors"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    behaviors:NavigationViewHeaderBehavior.HeaderMode="Never"
    mc:Ignorable="d">

    <ScrollViewer>
        <Grid x:Name="ContentArea" MaxWidth="800">
            <Grid.RowDefinitions>
                <RowDefinition Height="auto" />
                <RowDefinition Height="auto" />
                <RowDefinition Height="auto" />
                <RowDefinition Height="auto" />
                <RowDefinition Height="auto" />
                <RowDefinition Height="auto" />
                <RowDefinition Height="auto" />
            </Grid.RowDefinitions>
            <Grid
                Grid.Row="0"
                Margin="0,20,0,0"
                Padding="16"
                Background="{ThemeResource LayerFillColorDefaultBrush}"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                BorderThickness="1"
                CornerRadius="6">
                <Grid Grid.Row="0" Grid.Column="0">
                    <Grid.RowDefinitions>
                        <RowDefinition />
                        <RowDefinition />
                        <RowDefinition />
                        <RowDefinition />
                    </Grid.RowDefinitions>

                    <Grid.ColumnDefinitions>
                        <ColumnDefinition />
                        <ColumnDefinition />
                        <ColumnDefinition />
                    </Grid.ColumnDefinitions>

                    <TextBlock
                        Grid.Row="0"
                        Grid.Column="0"
                        Grid.ColumnSpan="3"
                        Margin="{StaticResource MediumBottomMargin}"
                        Style="{StaticResource TitleTextBlockStyle}"
                        Text="{Binding ProductName}" />

                    <StackPanel
                        Grid.Row="1"
                        Grid.Column="0"
                        Margin="{StaticResource MediumBottomMargin}"
                        HorizontalAlignment="Center"
                        Orientation="Horizontal">
                        <FontIcon
                            FontFamily="{StaticResource BootstrapIcons}"
                            FontSize="18"
                            Foreground="#0BE1FF"
                            Glyph="&#xF5B0;" />
                        <TextBlock
                            Margin="5,0,0,0"
                            Style="{StaticResource BodyStrongTextBlockStyle}"
                            Text="{Binding PriceTag}" />
                    </StackPanel>


                    <StackPanel
                        Grid.Row="1"
                        Grid.Column="1"
                        Margin="{StaticResource MediumBottomMargin}"
                        Padding="{StaticResource LargeLeftRightMargin}"
                        HorizontalAlignment="Center"
                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                        BorderThickness="2,0,2,0"
                        Orientation="Horizontal">

                        <FontIcon
                            FontFamily="{StaticResource BootstrapIcons}"
                            FontSize="18"
                            Foreground="Green"
                            Glyph="&#xF5EA;" />
                        <TextBlock
                            Margin="5,0,0,0"
                            Style="{StaticResource BodyStrongTextBlockStyle}"
                            Text="{Binding ShippingPrice}" />
                    </StackPanel>

                    <StackPanel
                        Grid.Row="1"
                        Grid.Column="2"
                        Margin="{StaticResource MediumBottomMargin}"
                        HorizontalAlignment="Center"
                        Orientation="Horizontal">

                        <FontIcon
                            FontFamily="{StaticResource BootstrapIcons}"
                            FontSize="18"
                            Foreground="Orange"
                            Glyph="&#xF685;" />
                        <TextBlock
                            Margin="5,0,0,0"
                            VerticalAlignment="Center"
                            Style="{StaticResource BodyStrongTextBlockStyle}"
                            Text="{Binding Stock}" />
                    </StackPanel>

                    <HyperlinkButton
                        Grid.Row="3"
                        Grid.Column="0"
                        Grid.ColumnSpan="3"
                        HorizontalAlignment="Right"
                        Content="Open in browser"
                        NavigateUri="{Binding ProductUrl}" />

                </Grid>
            </Grid>

            <Grid
                Grid.Row="1"
                Margin="0,20,0,0"
                Padding="16"
                Background="{ThemeResource LayerFillColorDefaultBrush}"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                BorderThickness="1"
                CornerRadius="6">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition />
                    <ColumnDefinition />
                </Grid.ColumnDefinitions>
                <StackPanel
                    Grid.Column="0"
                    HorizontalAlignment="Stretch"
                    BorderBrush="{ThemeResource CardStrokeColorDefault}"
                    BorderThickness="0,0,2,0">
                    <TextBlock
                        x:Uid="PreviousPrice"
                        HorizontalTextAlignment="Center"
                        Style="{StaticResource BodyStrongTextBlockStyle}" />
                    <TextBlock HorizontalTextAlignment="Center" Text="{Binding ShowMinProductPrice}" />
                </StackPanel>
                <StackPanel Grid.Column="1" HorizontalAlignment="Stretch">
                    <TextBlock
                        x:Uid="CurrentPrice"
                        HorizontalTextAlignment="Center"
                        Style="{StaticResource BodyStrongTextBlockStyle}" />
                    <TextBlock HorizontalTextAlignment="Center" Text="{Binding PriceTag}" />
                </StackPanel>
                <StackPanel
                    Grid.Column="2"
                    HorizontalAlignment="Stretch"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="2,0,0,0">
                    <TextBlock
                        x:Uid="PriceDifference"
                        HorizontalTextAlignment="Center"
                        Style="{StaticResource BodyStrongTextBlockStyle}" />
                    <TextBlock HorizontalTextAlignment="Center" Text="{Binding ShowMaxProductPrice}" />
                </StackPanel>
            </Grid>

            <Grid
                Grid.Row="2"
                Margin="0,20,0,0"
                Padding="16"
                Background="{ThemeResource LayerFillColorDefaultBrush}"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                BorderThickness="1"
                CornerRadius="6">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition />
                    <ColumnDefinition />
                </Grid.ColumnDefinitions>
                <StackPanel
                    Grid.Column="0"
                    HorizontalAlignment="Stretch"
                    BorderBrush="{ThemeResource CardStrokeColorDefault}"
                    BorderThickness="0,0,2,0">
                    <TextBlock HorizontalTextAlignment="Center" Style="{StaticResource BodyStrongTextBlockStyle}">Lowest Price</TextBlock>
                    <TextBlock HorizontalTextAlignment="Center" Text="{Binding ShowMinProductPrice}" />
                </StackPanel>
                <StackPanel Grid.Column="1" HorizontalAlignment="Stretch">
                    <TextBlock HorizontalTextAlignment="Center" Style="{StaticResource BodyStrongTextBlockStyle}">Averange Price</TextBlock>
                    <TextBlock HorizontalTextAlignment="Center" Text="{Binding ShowAvgProductPrice}" />
                </StackPanel>
                <StackPanel
                    Grid.Column="2"
                    HorizontalAlignment="Stretch"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="2,0,0,0">
                    <TextBlock HorizontalTextAlignment="Center" Style="{StaticResource BodyStrongTextBlockStyle}">Highest Price</TextBlock>
                    <TextBlock HorizontalTextAlignment="Center" Text="{Binding ShowMaxProductPrice}" />
                </StackPanel>
            </Grid>

            <Grid
                Grid.Row="3"
                Margin="0,20,0,0"
                Padding="16"
                Background="{ThemeResource LayerFillColorDefaultBrush}"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                BorderThickness="1"
                CornerRadius="6">
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto" />
                    <RowDefinition Height="auto" />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition />
                </Grid.ColumnDefinitions>
                <TextBlock
                    Grid.Row="0"
                    HorizontalAlignment="Stretch"
                    Style="{StaticResource SubtitleTextBlockStyle}"
                    Text="Notifications" />
                <TextBlock
                    Grid.Row="1"
                    HorizontalAlignment="Stretch"
                    Style="{StaticResource BodyTextBlockStyle}"
                    Text="Always notify me when the price changes" />
                <Button
                    Grid.Row="1"
                    Grid.Column="1"
                    Width="120"
                    HorizontalAlignment="Right"
                    Content="Edit" />
            </Grid>

            <Grid
                Grid.Row="4"
                Margin="0,20,0,0"
                Padding="16"
                Background="{ThemeResource LayerFillColorDefaultBrush}"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                BorderThickness="1"
                CornerRadius="6">

                <Grid.RowDefinitions>
                    <RowDefinition Height="auto" />
                    <RowDefinition Height="auto" />
                </Grid.RowDefinitions>

                <TextBlock
                    Grid.Row="0"
                    Style="{StaticResource SubtitleTextBlockStyle}"
                    Text="Price History" />

                <TextBlock
                    Grid.Row="1"
                    Style="{StaticResource BodyTextBlockStyle}"
                    Text="{Binding ProductHistory}" />
            </Grid>

            <Grid
                Grid.Row="5"
                Margin="0,20,0,0"
                Padding="16"
                Background="{ThemeResource LayerFillColorDefaultBrush}"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                BorderThickness="1"
                CornerRadius="6">
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto" />
                    <RowDefinition Height="auto" />
                </Grid.RowDefinitions>

                <FlipView
                    x:Name="flip"
                    Grid.Row="1"
                    Background="White"
                    BorderBrush="Black"
                    BorderThickness="1"
                    ItemsSource="{Binding ListImages, Mode=TwoWay}"
                    Loaded="flip_Loaded">
                    <FlipView.ItemTemplate>
                        <DataTemplate>
                            <Image
                                MaxWidth="800"
                                Source="{Binding}"
                                Stretch="Uniform" />
                        </DataTemplate>
                    </FlipView.ItemTemplate>
                </FlipView>
                <!--  TextWrapping="Wrap"  -->
            </Grid>
            <Grid
                Grid.Row="6"
                Margin="0,20,0,5"
                Padding="16"
                Background="{ThemeResource LayerFillColorDefaultBrush}"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                BorderThickness="1"
                CornerRadius="6">
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto" />
                    <RowDefinition Height="auto" />
                </Grid.RowDefinitions>
                <TextBlock
                    Grid.Row="0"
                    Style="{StaticResource SubtitleTextBlockStyle}"
                    Text="Description" />
                <TextBlock
                    Grid.Row="1"
                    Style="{StaticResource BodyTextBlockStyle}"
                    Text="{Binding ProductDescription}" />
                <!--  TextWrapping="Wrap"  -->
            </Grid>
        </Grid>
    </ScrollViewer>
</Page>
