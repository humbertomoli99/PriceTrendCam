<Page
    x:Class="PriceTrendCam.Views.AddSitemapPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid
        x:Name="ContentArea">
        <Grid>
            <ScrollViewer>
                <StackPanel
                    HorizontalAlignment="Stretch"
                    VerticalAlignment="Top">
                    <Grid
                        MaxWidth="800">
                        <Grid.RowDefinitions>
                            <RowDefinition
                                Height="Auto" />
                            <RowDefinition
                                Height="1*" />
                        </Grid.RowDefinitions>
                        <StackPanel
                            Grid.Row="0"
                            Margin="0,16">
                            <Grid
                                Background="{ThemeResource LayerFillColorDefaultBrush}"
                                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                                BorderThickness="1,1,1,1"
                                CornerRadius="5,5,5,5">
                                <Grid.RowDefinitions>
                                    <RowDefinition
                                        Height="auto" />
                                    <RowDefinition
                                        Height="auto" />
                                </Grid.RowDefinitions>
                                <StackPanel
                                    Grid.Row="0"
                                    Width="172"
                                    Height="172"
                                    Margin="50"
                                    BorderBrush="#FFC5C2C2"
                                    BorderThickness="1,1,1,1"
                                    CornerRadius="5,5,5,5">
                                    <Image
                                        Width="170"
                                        Height="170"
                                        Source="{Binding Imagen}"
                                        Stretch="Uniform" />
                                </StackPanel>
                            </Grid>
                        </StackPanel>
                        <StackPanel
                            Grid.Row="1"
                            Padding="16"
                            Margin="0,16"
                            Background="{ThemeResource LayerFillColorDefaultBrush}"
                            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                            BorderThickness="1"
                            CornerRadius="6">
                            <StackPanel
                                Margin="0,6">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition
                                            Height="Auto" />
                                        <RowDefinition
                                            Height="*" />
                                    </Grid.RowDefinitions>
                                    <TextBlock
                                        Grid.Row="0">Sitemap Name</TextBlock>
                                    <TextBox
                                        Grid.Row="1"
                                        x:Name="StoreName"
                                        PlaceholderText="Sitemap Name"
                                        Text="{x:Bind StoreName.Text, Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" />
                                </Grid>
                            </StackPanel>
                            <StackPanel
                                Margin="0,6">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition
                                            Height="Auto" />
                                        <RowDefinition
                                            Height="*" />
                                    </Grid.RowDefinitions>
                                    <TextBlock
                                        Grid.Row="0">Start URL
                                    </TextBlock>
                                    <StackPanel
                                        Grid.Row="1"
                                        HorizontalAlignment="Stretch"
                                        Name="textBoxesStackPanel" />
                                </Grid>
                            </StackPanel>
                            <StackPanel
                                Margin="0,6">
                                <Button
                                    Click="Guardar_Click"
                                    Style="{StaticResource AccentButtonStyle}"
                                    Content="Add Sitemap">
                                </Button>
                            </StackPanel>
                        </StackPanel>
                    </Grid>
                </StackPanel>
            </ScrollViewer>
        </Grid>
    </Grid>
</Page>
