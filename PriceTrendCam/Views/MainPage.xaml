<Page
    x:Class="PriceTrendCam.Views.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid x:Name="ContentArea" VerticalAlignment="Top">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <StackPanel Grid.Column="0">
            <TextBox
                Height="60"
                FontSize="35"
                TextAlignment="Center"
                TextWrapping="NoWrap"
                PlaceholderText="Add product URL or search for product here"
                Text="{x:Bind ViewModel.TextBoxSearch, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
        </StackPanel>
        <StackPanel Grid.Column="1" VerticalAlignment="Bottom">
            <Button
                Width="60"
                Height="60"
                Margin="6,0,0,0"
                VerticalAlignment="Bottom"
                Command="{x:Bind ViewModel.AdvancedSearchCommand}"
                Content="🔎" />
        </StackPanel>
    </Grid>
</Page>
